{
  "config": {
    "step": {
      "user": {
        "title": "Configure Linky",
        "description": "Enter your authentication token from [{conso_url}]({conso_url})",
        "data": {
          "token": "Token"
        }
      },
      "select_prm": {
        "title": "Select meter",
        "description": "Your token grants access to multiple meters. Select the one you want to configure.",
        "data": {
          "prm": "Meter (PRM)"
        }
      }
    },
    "error": {
      "invalid_token": "The token is invalid or expired",
      "prm_access_denied": "This token does not grant access to this meter",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This meter is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Linky Options",
        "data": {
          "scan_interval": "Update interval"
        },
        "data_description": {
          "scan_interval": "How often to fetch data (in hours). Linky data has a 24-48h delay."
        }
      }
    }
  },
  "services": {
    "import_historical_data": {
      "name": "Import historical data",
      "description": "Import historical consumption and production data from a specific date",
      "fields": {
        "start_date": {
          "name": "Start date",
          "description": "Start date for data import (YYYY-MM-DD)"
        },
        "end_date": {
          "name": "End date",
          "description": "End date for import (YYYY-MM-DD). Default: today"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "daily_consumption": {
        "name": "Daily consumption"
      },
      "current_power": {
        "name": "Current power"
      },
      "max_power": {
        "name": "Maximum power"
      },
      "daily_production": {
        "name": "Daily production"
      },
      "current_production_power": {
        "name": "Production power"
      }
    }
  }
}
